package com.rodrigopeleias.bookstoremanager;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

//@Configuration
public class SwaggerConfiguration {

    /*@Bean
    public Docket docket(){
        return new Docket(DocumentationType.SWAGGER_2)      //tipo de documentacao
                .select()
                .apis(RequestHandlerSelectors.any())        //pegue todas as apis
                .paths(PathSelectors.any())                 //em todos os caminhos e gera um swagger
                .build();
    }*/
}